require 'spec_helper'

describe PollyPhone do
  it 'has a version number' do
    expect(PollyPhone::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
